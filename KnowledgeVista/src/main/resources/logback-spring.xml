<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Log directory -->
    <property name="LOG_PATH" value="logs" />

    <!-- Console Output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender for specific class log file -->
    <appender name="MODULE_TEST_LOG" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_PATH}/module-test.log</file>
        <append>true</append>

        <!-- ✅ Missing encoder added here -->
        <encoder>
            <pattern> %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for your class only -->
    <logger name="com.knowledgeVista.AiIntegration.AiService" level="DEBUG" additivity="false">
        <appender-ref ref="MODULE_TEST_LOG" />
    </logger>

    <!-- Root logger for other logs -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>
